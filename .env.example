# 这个文件是可选的第二条路。首选是浏览器登录：
#
#     ant auth login
#
# 登录之后凭据在 %APPDATA%\Anthropic\，SDK 自动读，这里什么都不用填。
#
# 想用固定 key 的话再解开下面这行 —— 但注意它会盖住已登录的 profile，
# 而且**空字符串也会盖**（照样占优先级，然后拿空 key 去请求）。
# 想切回登录凭据就把这一行整个删掉，不是留个空值。
# ANTHROPIC_API_KEY=sk-ant-...
#
# 多套凭据时选用哪一个（对应 ant auth login --profile <名字>）
# ANTHROPIC_PROFILE=default

# 可选：换数据目录（默认 %APPDATA%\jo-app）
# JOAPP_HOME=D:\jo-app-data
